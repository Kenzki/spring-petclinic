pipeline {
    agent any

    stages {
        stage ('Compile Stage') {

            steps{
                withMaven(maven : 'Maven3.6.3') {
                    sh 'mvn clean compile'

                }
            }
        }

}
}
