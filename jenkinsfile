pipeline {
    agent any

    stages {
        stage ('Compile Stage') {

            steps{
                withMaven(maven : 'Maven3.6.3') {
                    bat 'mvn clean compile'

                }
            }
        }
    }
}
